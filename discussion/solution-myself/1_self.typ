= 1.1 Concept Check

== 1.

This question is more compilcate than its answer...

The first guy is virtual memory, which isolate each process, prevent the process to access other's (each process has his own space, register, fd ...)

Second, dual-mode operation, only kernel mode can exectue privilege instruction. 

And next timer interrupt, hardware will periodlically trigger a non-maskable interrupt to check process execution.

== 2.

